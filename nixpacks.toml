# Pin Node 22; Nixpacks installs pnpm from root package.json "packageManager".
[variables]
NIXPACKS_NODE_VERSION = "22"

[install]
cmds = ["pnpm install --frozen-lockfile"]

[build]
cmds = ["pnpm run typecheck && pnpm run build:api-server"]
