# Align with railway.toml: install from monorepo root, then build the API bundle.
[install]
cmds = ["pnpm install --frozen-lockfile"]

[build]
cmds = ["pnpm run typecheck && pnpm run build:api-server"]
