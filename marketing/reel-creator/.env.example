# Optional gate for /api/reel-builder (must match api-server REEL_BUILDER_API_KEY).
# Leave unset locally if the API key is also unset.
VITE_REEL_BUILDER_API_KEY=

# api-server origin for Vite proxy (default http://127.0.0.1:8787)
VITE_API_ORIGIN=http://127.0.0.1:8787
