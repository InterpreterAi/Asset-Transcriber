# InterpreterAI Reel Builder

Standalone marketing reel builder (from the new reel-builder app).

Uses the **real** InterpreterAI brand logos from `public/brand/` (copied from `marketing/brand/assets`).

## Run

```bash
cd marketing/reel-creator
npm install
npm run dev
```

→ http://localhost:5179
