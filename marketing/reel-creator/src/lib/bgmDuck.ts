/** Shared BGM ducking ratios for preview mixer + MP4 export (Reel Builder). */

/** While voiceover is speaking — ~15–20% of the BGM slider. */
export const BGM_DUCK_WHILE_VO = 0.18;

/** During outro silence / pad after VO — raise slightly above duck. */
export const BGM_OUTRO_PAD_RATIO = 0.42;

/** Full unduck (no VO). */
export const BGM_FULL_RATIO = 1;
