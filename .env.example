# Local API (artifacts/api-server): copy to `.env` or `.env.local` at the **repository root**
# so Drizzle and the API see the same vars. Restart `pnpm run dev` / api-server after editing.

# Postgres (local or tunnel URL)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DBNAME

# Public URL for OAuth redirects and email links (local UI is usually http://localhost:5173)
APP_URL=http://localhost:5173

# Resend — required for signup verification, password reset, support mail, and scheduled reminders
RESEND_API_KEY=

# Soniox — required for live transcription (/api/transcription/token)
SONIOX_API_KEY=
# Aliases: SONIOX_STT_API_KEY, SONIOX_KEY, SONIOX_API_TOKEN (case-insensitive env names OK)

# *-libre machine translation: default base is http://libretranslate.railway.internal:5000 (see libretranslate.ts).
# Override if your internal hostname/port differ (full URL, e.g. http://my-libre.railway.internal:5000).
LIBRETRANSLATE_INTERNAL_URL=
# Legacy alias; same override (schemeless non-.railway.internal hosts default to https).
LIBRETRANSLATE_URL=
LIBRETRANSLATE_API_KEY=

# OpenAI translation cost: optional extra multiplier on top of verified-window reconciliation in transcription.ts
# (sum of manual Apr 3–18 dailies / $50). Example: OPENAI_TRANSLATION_COST_CALIBRATION=1.15
# OPENAI_TRANSLATION_COST_CALIBRATION=
