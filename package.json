{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "npm install --prefix artifacts/api-server && npm run build --prefix artifacts/api-server",
    "start": "node artifacts/api-server/dist/index.js",
    "dev": "NODE_ENV=development npm run build && npm run start"
  },
  "dependencies": {
    "stripe": "^21.0.1",
    "stripe-replit-sync": "^1.0.0"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.9.2"
  }
}
