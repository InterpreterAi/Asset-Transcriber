{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter \"./artifacts/api-server\" build",
    "start": "node artifacts/api-server/dist/index.js",
    "dev": "pnpm --filter \"./artifacts/api-server\" dev"
  },
  "dependencies": {
    "stripe": "^21.0.1",
    "stripe-replit-sync": "^1.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "typescript": "^5.4.2"
  }
}
