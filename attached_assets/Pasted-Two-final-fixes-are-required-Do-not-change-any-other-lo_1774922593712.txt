Two final fixes are required. Do not change any other logic.

Important:  
Do NOT modify transcription, diarization, segmentation, translation timing, or UI layout.

Only implement the following behavior fixes.

--------------------------------
1. DO NOT DELETE GREY TRANSCRIPT TEXT
--------------------------------

Current problem:
If the transcript text is still grey (interim text) and the user presses Stop Listening, the grey transcript disappears but the translation remains.

Required behavior:

• If Stop Listening is pressed while grey transcript text exists, convert the grey transcript into a finalized segment instead of deleting it.  
• Preserve the transcript text exactly as it appears.  
• Preserve the translation that already exists.  
• Do NOT clear or erase interim transcript text.

Result:
Stopping listening should never erase spoken words.

--------------------------------
2. STRICT LANGUAGE PAIR ENFORCEMENT
--------------------------------

Current problem:
When a third language is spoken that is not part of the selected language pair, the system sometimes still attempts translation.

Required behavior:

• Detect the language of every transcript segment.  
• Compare the detected language with the two selected languages in the UI.

If detected language = Language A → translate to Language B.  
If detected language = Language B → translate to Language A.

If detected language is NOT Language A or Language B:

• Do NOT translate.  
• Copy the original transcript text directly into the translation column unchanged.

Example:

Selected pair:
English ↔ Arabic

Speaker says Spanish:
"Hola amigo"

Translation column must show exactly:
"Hola amigo"

Never translate third languages.

--------------------------------
3. PREVENT LATE LANGUAGE CORRECTIONS
--------------------------------

Once the language of a segment is detected and translation decision is made, lock that decision.

Do NOT re-detect language later and change the translation afterwards.

This prevents the system from first translating and then removing or changing the translation.

--------------------------------
Important:
Do not modify real-time transcription streaming, segmentation timing, or translation debounce behavior. Only apply these logic fixes.